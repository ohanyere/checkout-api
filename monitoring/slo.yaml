apiVersion: sre.platform.ohanyere.io/v1alpha1
kind: ServiceLevelObjective
metadata:
  name: checkout-api-availability
  labels:
    app.kubernetes.io/name: checkout-api
    finops.ohanyere.io/cost-center: platform-engineering
spec:
  service: checkout-api
  owner: Platform Engineering
  objective: 99.9
  window: 30d
  indicator:
    type: availability
    goodEvents: sum(rate(http_requests_total{service="checkout-api",status!~"5.."}[5m]))
    totalEvents: sum(rate(http_requests_total{service="checkout-api"}[5m]))
  alerting:
    burnRate:
      enabled: true
