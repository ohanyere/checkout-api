apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: checkout-api
  labels:
    app.kubernetes.io/name: checkout-api
spec:
  hosts:
    - checkout-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: checkout-api
            port:
              number: 80
          weight: 100
